{% extends 'base.html.twig' %}

{% block title %}ExampleEntity{% endblock %}

{% block body %}
    <h1>ExampleEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ example_entity.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_example_entity_index') }}">back to list</a>

    <a href="{{ path('app_example_entity_edit', {'id': example_entity.id}) }}">edit</a>

    {{ include('example_entity/_delete_form.html.twig') }}
{% endblock %}
